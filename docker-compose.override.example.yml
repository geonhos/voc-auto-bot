# Development overrides - restores port bindings for local debugging
# Copy this file to docker-compose.override.yml to use:
#   cp docker-compose.override.example.yml docker-compose.override.yml
# docker-compose.override.yml is automatically loaded by docker compose.
services:
  postgres:
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
  redis:
    ports:
      - "${REDIS_PORT:-6379}:6379"
  minio:
    ports:
      - "9000:9000"
      - "9001:9001"
  mailhog:
    ports:
      - "1025:1025"
      - "8025:8025"

  # Ollama: gpu 프로필에서만 사용 (docker compose --profile gpu up -d)
  # Mac 개발 환경에서는 네이티브 Ollama 사용 권장:
  #   brew install ollama && ollama serve
  #   ollama pull bge-m3 && ollama pull exaone3.5:7.8b
  # ollama:
  #   ports:
  #     - "${OLLAMA_PORT:-11434}:11434"
